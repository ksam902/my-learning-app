import { Block } from 'payload'

export const RecentActivity: Block = {
  slug: 'recentActivity',
  interfaceName: 'RecentActivityBlock',
  fields: [], // No fields needed as we're pulling data directly
}
